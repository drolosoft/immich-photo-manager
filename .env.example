# Immich MCP Server Configuration
IMMICH_BASE_URL=http://your-immich-server:2283
IMMICH_API_KEY=your-api-key-here
MCP_PORT=8626
# Extra Host header values to accept in HTTP mode (needed behind a reverse proxy)
# MCP_ALLOWED_HOSTS=immich-mcp.example.com
