"""immich-mcp-server: the MCP server behind the immich-photo-manager plugin.

`__version__` is the single source of the version string; the plugin manifest
and pyproject carry the same number and the release gate keeps the three in sync.
"""

__version__ = "1.12.0"
